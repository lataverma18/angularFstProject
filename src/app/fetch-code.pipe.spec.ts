import { FetchCodePipe } from './fetch-code.pipe';

describe('FetchCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchCodePipe();
    expect(pipe).toBeTruthy();
  });
});
